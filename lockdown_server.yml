---
- name: Security Lock-down for openstack hosts
  hosts: openstack
  user: rob
  become: true
  # vars:
  #   ansible_sudo_password: berty17c

  roles:
    - security
    - ansible-shorewall
    - certificates
    - email
    - logwatch

---
- name: Security Lock-down for openstack hosts
  hosts: openstack
  user: rob
  become: true
  # vars:
  #   ansible_sudo_password: berty17c

  roles:
    - security
    - certificates
    - email
    - logwatch
    - ansible-shorewall